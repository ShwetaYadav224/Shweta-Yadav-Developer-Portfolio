import SectionTitle from "../../components/common/SectionTitle";

export default function Projects() {
    return (
        <div className="w-full h-screen">
            <SectionTitle title="Projects" subtitle="Get to know me better" />
        </div>
    );
}
